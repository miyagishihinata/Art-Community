<div class="container">
  <h4 class="font-weight-bold text-muted mt-4 ml-5">通知</h4>
</div>

<div class="container">
  <% if @notices.exists? %>
    <div class="users-index">
      <%= render @notices %>
    </div>
  <% else %>
    <p>通知はありません</p>
  <% end %>
</div>

