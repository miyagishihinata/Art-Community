<%# 通知一覧 %>
<div class="container">
  <h4 class="font-weight-bold text-muted mt-4 ml-5">通知</h4>
</div>

<div class="container">
  <% if @notices.exists? %>
    <div class="users-index">
      <%= render partial: 'public/notices/notice', collection: @notices, as: :n %>
    </div>
  <% else %>
    <p>通知はありません</p>
  <% end %>
</div>

